server:
  port: 5678
spring:
  main:
    banner-mode: off
  application:
    name: sidecar
  profiles:
    active: ${ENV:default}
  cloud:
    config:
      uri: http://${CONFIG_HOST:localhost}:8888
      fail-fast: true
sidecar:
  port: 8080
  health-uri: http://localhost:8080/health
